<IfModule mod_rewrite.c>
  RewriteEngine On

  # Minden kérést átirányítunk a public mappába
  RewriteCond %{REQUEST_URI} !^/public/
  RewriteCond %{REQUEST_URI} !^/resources/
  RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
